{
  "name": "fitactive-server",
  "version": "0.0.1",
  "description": "FitActive presales backend server with payment processing and invoice generation",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "NODE_ENV=development node server.js",
    "test:server": "NODE_ENV=test node server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "echo \"Linting not configured\" && exit 0",
    "env:dev": "NODE_ENV=development node server.js",
    "env:test": "NODE_ENV=test node server.js",
    "env:prod": "NODE_ENV=production node server.js"
  },
  "keywords": [
    "fitness",
    "presales",
    "payment",
    "netopia",
    "smartbill"
  ],
  "author": "FitActive Team",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.7.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^6.9.14",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "netopia-payment2": "^0.1.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}